namespace Vaelastrasz.Library.Extensions
{
    public static class ConfigurationExtensions
    {
        //public static Configuration AddPlaceholder(this Configuration config, string key, string value)
        //{
        //    if (config.Placeholders.ContainsKey(key))
        //        return config;

        //    config.Placeholders.Add(key, value);
        //    return config;
        //}

        //public static Configuration RemovePlaceholder(this Configuration config, string key)
        //{
        //    if (!config.Placeholders.ContainsKey(key))
        //        return config;

        //    config.Placeholders.Remove(key);
        //    return config;
        //}

        //public static Configuration SetPlaceholders(this Configuration config, Dictionary<string, string> placeholders)
        //{
        //    config.Placeholders = placeholders;
        //    return config;
        //}
    }
}